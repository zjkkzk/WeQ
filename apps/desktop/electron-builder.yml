appId: app.weq.desktop
productName: weQ
copyright: Copyright © 2026 weQ contributors
directories:
  output: release/${version}
  buildResources: build
files:
  - out/**/*
  - package.json
extraResources:
  # native (.node) files live outside the source tree and are bundled at
  # package time. Source uses the nested layout `native/<os>/<arch>/*.node`
  # (e.g. native/win32/x64/nt_helper.node); destination keeps the same shape.
  - from: ../../native/${os}/${arch}
    to: native/${os}/${arch}
    filter:
      - '*.node'
  # Shared static assets (brand/logo, future QQ emoji, …). Copied whole so the
  # main process can read them at runtime via process.resourcesPath/resources.
  - from: ../../resources
    to: resources
asarUnpack:
  - resources/native/**/*
win:
  icon: ../../resources/brand/logo.png
  target:
    - target: nsis
      arch:
        - x64
  artifactName: ${productName}-${version}-win-${arch}.${ext}
nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  artifactName: ${productName}-${version}-setup.${ext}
